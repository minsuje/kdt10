import Button from './Button';
import Panel from './Panel';

function Form() {
    return (
        <Panel title="welcome">
            <Button>sing up</Button>
            <Button>Log in</Button>
        </Panel>
    );
}

export default Form;
